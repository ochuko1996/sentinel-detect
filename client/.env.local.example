# Base URL of the sentinel-detect API backend (see ../api).
# Must be reachable from the browser — the API's CORS config
# (SENTINEL_SECURITY__CORS_ORIGINS in the backend's .env) must allow this
# origin (defaults to http://localhost:3000, which matches `npm run dev`).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
